package autobackup.GUI;

import javafx.fxml.FXML;
import javafx.scene.control.ListView;

/**
 * Dies ist der Controller für das Fenster in dem die Ordner angezeigt werden die auf der Blacklist sind.
 * @author ASFDGamer
 */
public class BlacklistedFolderController extends GUIController
{
    
    /**
     * Dies ist die Listview in der alle Ordner angezeigt werden sollen die auf der Blacklist stehen.
     */
    @FXML
    private ListView BlacklistedOrdner;
}
